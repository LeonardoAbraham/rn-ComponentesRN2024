/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import { ComponentsApp } from './src/ComponetsApp';

AppRegistry.registerComponent(appName, () => ComponentsApp);
